# Aurex — handoff

**Read this before touching anything.** It is the state of the project as of 2026-08-14, written so a new session can pick up without re-deriving context. The rules in §1 are not stylistic preferences; they are the reason the project has any credibility, and every one of them was written after something went wrong.

- **Repo:** https://github.com/MadScientistX92/aurex (public)
- **Dashboard:** https://aurex-five.vercel.app
- **Local:** `~/aurex` on a Mac mini. Engine in `engine/`, dashboard in `web/`, artifacts in `public-data/`.
- **Built with:** Claude Code, manual-approve mode. Agent memory lives in the Claude Code project memory directory.

---

## 1. The working method — diagnose, never assume

**This is the most important section. If you read nothing else, read this.**

When something fails, when a number looks wrong, or when you are about to explain a result: **investigate before asserting.** Read the log. Run the query. Check the artifact. Say "I don't know yet" and go find out. Never reason from what a failure *probably* was, never fill a gap with a plausible-sounding mechanism, and never propose a fix built on an unverified diagnosis.

This is not caution for its own sake. It is empirical. Across the build, the human collaborator advanced roughly **thirteen** confident hypotheses and **the data killed essentially all of them**:

| Claimed | Actually |
|---|---|
| India gold import duty is 6% | 15% (BCD 10 + AIDC 5), from May 2026 |
| CBIC Notification 27/2026 | 15–18/2026, dated 12 May 2026 |
| HS headings 7107/7109/7110/7111/7112/7118 | Two different lists — and gold bullion is **7108**, in neither |
| Touch-probability ≈ 2× terminal | 1.35× at 10 sessions (continuous vs discrete monitoring) |
| "The FHS mean is zero by construction" | Drift leaks back through undemeaned residuals |
| DM truncation lag = h−1 | Must derive from the sampling step; h−1 over-specifies at step>1 |
| 10-day σ ≈ 4.4% (pre-registration) | ≈3.2%; 4.4% came from crisis-period vol |
| Tail closes 9/10 of the Gaussian shortfall | Inverted: 91% volatility level, 9% tail shape |
| Vercel Root Directory = repo root | Must be `web/` with include-outside-files enabled |
| 2025 is the load-bearing LOYO year | **2017** is; dropping it takes p from 0.0069 → 0.6528 |
| Tie-ordering inflates the h=10 result | Backwards — position ordering made it look *weaker* |
| Cyclic shift under-represents trend alignment | It **over**-represents it on this sample |
| CI failure was the GPR/DesignError path | Price-series path; GPR resolved fine and never entered |

Not one of those reached the codebase as a wrong fix, because each went to a diagnostic prompt rather than an implementation prompt. **That is the entire method.** Preserve it.

The agent side has the same discipline and the same track record. Bugs caught by *reading output* rather than by a green test suite: `df_at_bound` at 59.9994 reporting `False`; a double FX division putting the rupee anchor at ₹330 instead of ₹17,784; `applies_at` scoring a hurdle at horizons it was never priced for; a `?? 1` fallback publishing a p-value of 1.00; the root `.gitignore` silently swallowing `web/lib/`; a README figure of 0.010 against an artifact value of 0.0095. **A green suite is not evidence. Look at the numbers.**

### Corollaries

- **A missing value that reads as a stronger claim is the signature failure mode here.** `?? 1` → "definitively no effect". A NaN → SPA p = 0.000, maximally significant. A `False` from a boundary check → "found fat tails". Hunt absences that read as confidence, not just crashes.
- **Report what you measured, not what the spec predicted.** When a brief says "expect roughly double" and you measure 1.35, report 1.35 and explain the gap. Never tune toward the brief.
- **A change is not made until it is pushed.** Reporting a change as done while it sits uncommitted in the working tree is the same class of error as a published number nobody can reproduce. This has happened; it cost two failed deploys.
- **Say when you are unsure.** "This is the leading hypothesis, not a proven mechanism, and it stays unproven until someone captures the body" is exactly the right register.

---

## 2. What Aurex is (§0, the philosophy)

> Short-horizon price **direction** is not reliably forecastable. Short-horizon **volatility** partly is. So Aurex never predicts a price — it produces a probability distribution, and then publicly grades how well-calibrated that distribution turned out to be.

Concretely, and enforced by tests:

- **No point forecasts anywhere.** Not in the API, not in the UI, not in the README. A number without an interval or a distribution behind it is a bug.
- **Every probability is scored.** A forecast that is never scored is marketing.
- **The null is the driftless random walk.** Any model that does not beat it ships anyway, labelled as not beating it. Negative results are published, not buried.
- **No overclaiming.** A CI guard greps user-facing text for a curated list of banned phrases: marketing vocabulary claiming to outperform a benchmark or to carry professional credentials, promises of certain gain or of no downside, and point-forecast language naming a level with no distribution behind it. The list is in `engine/tests/test_no_overclaiming.py`, which is deliberately the only file exempt from its own scan — the phrases are not repeated anywhere else, this line included, because a document that quotes them is a document the guard would have to be widened around rather than widened onto.
- **Every published figure is mechanically tied to the artifact that produced it.** `tests/test_readme_direction.py` and `tests/test_readme_factors.py` parse the README cell by cell against the JSON and fail the build on disagreement. This mechanism has already caught a real error.

---

## 3. Build state

**Done:** steps 1, 1.5, 2, 3a, 3b, 4, 6, 7, and most of 5.

| Step | What |
|---|---|
| 1 | Data layer, source chains with parquet cache, dated duty/GST schedules, import parity, `local_premium_bps` |
| 1.5 | Currency lenses (USD native, INR taxed-import), asset abstraction (`Asset` protocol) |
| 2 | GJR-GARCH / HAR-RV / rolling-std; filtered historical simulation with **paths preserved**; t-copula; first-passage statistics |
| 3a | PIT, CRPS, Kupiec (exact binomial at boundaries), Christoffersen, Diebold-Mariano with HAC |
| 3b | Routes × jurisdictions, friction profiles, breakeven hurdle, hurdle-clearing Brier |
| 4 | Elastic-net factor loadings, GPR index wired, crude→CPI→policy→rupee chain by local projections, fuel-excise control |
| 5 | Dashboard: **Today**, **Track record**, **Calculator**. Drivers and Scenarios **omitted, not stubbed** |
| 6 | Benchmark shootout (RW, GJR-GARCH, HAR-RV, AutoARIMA, NHITS, Chronos) + direction grading with SPA/MCS and minimum detectable effect |
| 7 | CI, nightly job with staleness refusal, track-record integrity, live log |

**Not built:** the scenario engine (remainder of step 5), the Drivers and Scenarios dashboard views (blocked on scenario work), and oil as a second asset (steps 8–9, specified in addenda §17/§18).

---

## 4. Results as they stand

**Calibration — the headline is a negative result.** Across 2,876 out-of-sample forecasts from January 2015, CRPS skill against the random walk runs between −0.4% and +0.9% depending on horizon, and Diebold-Mariano rejects at **none** of them; the smallest p in the table is 0.23. PIT uniformity survives KS at all five horizons; chi-square rejects at one.

**A withdrawn number.** An earlier version reported up to +4.6% CRPS skill. That was a model carrying sample drift beating a null denied one. It has been withdrawn, the residual pool is now centred by default, and — measured afterwards — it was never significant anyway (p = 0.27). Centring made *every* published number worse and it was taken anyway.

**Direction carries no information.** Resolution at or near zero for every model at every horizon, including Chronos zero-shot. One cell (HAR-RV, h=10, equal-count screen) rejects at p = 0.036 / 0.025 thinned. It is published as a finding and read as noise, for three stated reasons: multiplicity across ten uncorrected cells; effect size of 0.011 resolution against 0.248 uncertainty; and leave-one-year-out showing **2017 is load-bearing** — dropping that year alone takes the marginal p from 0.0069 to 0.6528 and puts the resolution below its own null. Two earlier published reasons were withdrawn after controls contradicted them, and the correction is visible in the README rather than smoothed over.

**Tail shape.** The claim that filtered historical simulation closed "nine tenths of the Gaussian shortfall" was **inverted** on proper baselining. Held to the engine's own forecast variance, the split is 91% volatility level and 9% tail shape, and on seven of nine rows the engine is *thinner*-tailed than a variance-matched Gaussian (skew −0.38). Corrected in all three places that inherited it.

**Step 4 against its pre-registration.** Predictive out-of-sample R² = −0.00218, DM p = 0.8346 — **hit**. Contemporaneous R² = 0.18699 — **missed**, just below the registered 0.2–0.45. Stability **split**: five of six drivers change sign across rolling windows, but the rolling spread never exceeds the full-sample interval. Chain band spans zero at every horizon — **hit**. The two routes disagree (+0.0027 chain-implied vs −0.13401 direct at six months) — **hit**, published as a finding and not reconciled.

**The GPR result is the most instructive.** The geopolitical-risk index was wired as a hard prerequisite specifically to prevent an inverted safe-haven sign. Removing it moves the largest surviving loading by **0.0000157** — twenty-five times less than any other driver — with no sign flips anywhere, and out-of-sample R² goes *up*. The bias identified in advance **did not materialise in the loadings on this sample**. That is published as the result. "We checked and it was negligible" and "we did not check" produce identical coefficients and are not the same claim.

**Friction is the thesis.** Same metal, same distribution, same day: P(profit) is 0.483 through an oil-style CFD route and 0.089 through Indian retail. Breakeven hurdle 9.37% (India retail) vs 0.14% (CFD). The hurdle is irrelevant at low friction and decisive at high friction — that is the whole point, and the CFD route's 293 "clears" at five sessions are 293 coin flips landing heads, not 293 wins.

---

## 5. The open problem — start here

`xauusd`, the anchor price series that every published price and every simulated path is built from, **has a source chain of length one**: `LbmaGoldLoader`. `usdinr` looks better — Yahoo plus a FRED fallback — but that redundancy has since been measured and is nominal; see below. The one blocking series with no second source is the one everything depends on, and the one that appears to have a second source has one that cannot arrive in time.

From GitHub runners it fails roughly **56% of the time** (4 resolved, 5 failed, 2026-08-05 to 08-13) with a 2xx response whose body is not JSON. From a residential IP it answered 12/12. On **2026-08-13 a runner resolved it at 04:05 UTC and another failed on it at 19:12** — same endpoint, same day — so this is per-run, not an outage anyone could wait out.

**Mechanism, settled.** Probe run 31906804255: 20/20 attempts returned HTTP 200 carrying a complete ~12KB Cloudflare interstitial ("One moment, please…", `cf-edge-cache: no-cache`), 20 distinct bodies, none converting. Not a truncated body. Its only remedy is a JavaScript reload after five seconds, and `http.get` calls the module-level `requests.get`, which builds and discards a `Session` per call — so no state survives to be sent back and **a courtesy-delayed retry is mechanically dead**. Probe run 31974956815 then answered 5/5 clean from a different runner IP, which fixes the shape of the problem: per-egress, not per-day. The repair is a second source on a different host.

**The candidate landscape is thin, and that is a finding rather than a gap in the search.** FRED's LBMA fix was removed with all ICE Benchmark Administration data on 2022-01-31. ICE, which administers the benchmark, publishes no unauthenticated endpoint at all — delivery is ICE Connect / Data API / Data Files / Global Network, under licence, contact `iba-licensing@ice.com` — and LBMA moved its own historic tables to the members' portal in the week commencing 2025-11-24, keeping only the latest daily auction price and chart data public. The Bundesbank's directory holds exactly one gold price series, `BBEX3.D.XAU.DEM.EA.AC.C01`, the Frankfurt fixing in D-marks per kilo, last observation **1998-12-30**. stooq.com and fsapi.gold.org publish `User-agent: * / Disallow: /`. fxratesapi serves a runner but is rejected on the data: weekend carry-forward that would satisfy the freshness guard with a repeated Friday quote, ≈ −0.34% against the PM fix, wrong fixing time, and a 366-day history wall. **The status of the LBMA JSON feed under the portal move is itself unresolved, and an enquiry to LBMA is drafted at `docs/lbma-enquiry.md`.**

### The second single-source fragility: `usdinr`'s fallback is nominal

Measured 2026-08-17, and the reason it matters is that `usdinr` is *blocking* — the rupee lens converts through it, so a nightly that cannot resolve it publishes nothing.

The fallback is FRED `DEXINUS`, which carries daily observations but is **published weekly**. Evidence, not inference: 76 consecutive ALFRED vintages (2026-06-02 → 2026-08-16, no errors) show the newest available observation changing on exactly 11 dates, every one a Monday except 2026-06-02, which followed a Monday holiday — and each release reaches only the **preceding Friday**. The Federal Reserve's own H.10 schedule says the same: released "On Mondays at 4:15 p.m.", next business day if Monday is a holiday, covering "the previous business week".

`usdinr` declares `max_lag_days=4` (`engine/aurex/assets/gold.py`), and the engine refuses when `(run_date − last_observation).days > 4` (`engine/aurex/data/freshness.py`). The nightly runs at 02:00 UTC, *before* the 20:15 UTC Monday release. So the lag a nightly would actually see is:

| Night (02:00 UTC) | On-time week (release Mon 20:15 UTC) | Lag | Holiday week (release slips to Tue) | Lag |
|---|---|---|---|---|
| Mon | Friday of the week before last | 10 | Friday of the week before last | 10 |
| Tue | previous Friday | **4 — clears** | still Friday of the week before last | 11 |
| Wed | previous Friday | 5 | previous Friday | 5 |
| Thu | previous Friday | 6 | previous Friday | 6 |
| Fri | previous Friday | 7 | previous Friday | 7 |
| Sat | previous Friday | 8 | previous Friday | 8 |
| Sun | previous Friday | 9 | previous Friday | 9 |

**≈12% of nights, not one in seven.** An on-time week clears on exactly one night, Tuesday, at the boundary. A week whose release slips to Tuesday clears on **no night at all**: the Tuesday run happens before that day's release and sees a lag of 11, and by Wednesday the newly released data is already 5 days old. Nine of the eleven release weeks measured were on time, so the rate is **9 clearing nights in 77 ≈ 12%**. On every other night, if Yahoo `INR=X` fails, the chain resolves `DEXINUS` successfully and the freshness guard then correctly refuses it, so the run skips exactly as if there were no fallback at all. The redundancy is real in the source chain and absent in the freshness budget: the tolerance was derived for a daily-published FX series, which the primary is and the fallback is not.

**Do not fix this by moving the tolerance.** The number is right for what it guards. The honest repairs are a fallback that publishes daily, or a declared acceptance that `usdinr` is single-sourced in practice.

### Current branch: `fix-skip-reason-and-probe-lbma` (pushed, **not merged**)

Two commits:
- `a2907e4` — skip reasons derived from verdicts instead of a hardcoded string, plus `http.get_json` carrying status, headers (`content-type`, `server`, `cf-ray`, `cf-mitigated`, `cf-cache-status`), byte count and the first 200 bytes `repr`'d.
- `ab29bfb` — `.github/workflows/probe-lbma.yml`, a **throwaway** 20-attempt probe that records what a runner actually receives. `permissions: contents: read`; writes nothing to the repo. Delete once the mechanism is known.

**The probe cannot be dispatched from a branch** — GitHub only offers `workflow_dispatch` for workflows present on the default branch. It must land on `main` first.

### Immediate sequence

1. Merge `fix-skip-reason-and-probe-lbma` to `main`, push.
2. Dispatch **Probe LBMA** (`gh workflow run probe-lbma.yml`, or Actions tab → Probe LBMA → Run workflow).
3. Download the workflow artifact and **read it before proposing anything.**
4. Then decide the repair: second source (different host; **GC=F is excluded** — its cost-of-carry basis, +2.40% on 2026-07-29, would land in the domestic premium and read as a demand signal) versus courtesy-delayed retry.
5. Delete the probe workflow.
6. Separately: seed cache for `factors.yml` only — see §6.

### Also outstanding

- **`direction.yml` executed for the first time on 2026-08-17 (run 31983796753) and was killed at 94%. 360 minutes is the platform's cap, not ours, so the ceiling cannot be raised.** Verified 2026-08-17 against GitHub's own limits reference: *"Each job in a workflow can run for up to 6 hours of execution time. If a job reaches this limit, the job is terminated and fails"* — https://docs.github.com/en/actions/reference/actions-limits, and the same sentence at /en/actions/reference/limits. 360 is also the default for `timeout-minutes`, so the line in the workflow restates the cap rather than choosing it; the two coincide, which is why the cancellation cannot be attributed to one rather than the other, and why that attribution does not matter. **Whatever `timeout-minutes` says, a hosted runner terminates the job at six hours.** The job's own clock agrees to the second: started 01:03:23Z, completed 07:03:38Z — six hours and fifteen seconds, the fifteen being how long the kill took.
  - **So a full `--step 5` direction run is structurally impossible on a GitHub-hosted runner.** It needs ≈6.4 hours (547 windows in 5h58m, ~39.3s each, against the 580 the committed artifact implies) and the platform allows 6.0. It was ~23 minutes short, and there is no configuration that buys those 23 minutes. Raising the ceiling is not an option and must not be attempted; the honest choices are to **cut the work** (`--step 10` halves the windows to 290, or drop models, or drop horizons), to **split the sample across jobs** and combine, to move to a **self-hosted runner** (5 days, same reference), or to run it locally and commit the artifact by hand. The "~2h wall time" figure elsewhere in this section was a local-machine number; a Mac mini does ~11s a window against a 4-vCPU runner's ~39s.
  - The NHITS seed advancing by `step` × 1,000 per window is how that log could be read at all. `aurex direction` now prints its own counter — windows done over windows planned, elapsed, per-window rate and a projected finish, one line per window, flushed to **stderr** so `--dry-run`'s JSON on stdout stays parseable. See `aurex/score/progress.py`; the denominator is derived from where the loop stops rather than from `range(first, total, step)`, and `tests/test_progress.py` asserts the two agree over a grid where they can disagree. A projected finish in the first ten minutes is what makes the cap a decision instead of a surprise.
  - **Its step ordering is now fully tested.** In the killed run, steps 7, 8 and 10 — guard, README test, commit — were **skipped**, `git add` never ran, and `origin/main` carried no direction commit. Run **32044823578** (2026-08-17, `main`'s workflow, `--step 10 --to 2026-07-29`, `commit=false`, 3h14m — comfortably inside the cap) then exercised the rest for the first time: **grade succeeded, the guard passed, the README test failed, the upload ran and shipped a genuine artifact (sha256 `0d29636b…`, not the decoy `f1a34f34…`), and the commit step was correctly skipped.** So the ordering is confirmed end to end in both directions: a failure after the compute publishes an artifact and commits nothing.
    - The guard passing is the expected result, not a lucky one: it asserts structure — bounded sample, the drift-matched null, a resolution and `occupied_bins` per model, four screens present — none of which depends on the step. Confirmed independently by running both `main`'s and this branch's guard scripts against the artifact locally; **the branch's version, with its three `resolution.*` assertions, has now been exercised too**, which the branch's own workflow could not do.
    - The README test failed **6 of 11**, first on `assert 580 == 290` — the window count, before a single p-value is compared. So what fails step 8 is the sample size, not numeric drift. The other five: the placebo marginal *p*, the within-year cyclic-shift *p*, the leave-one-year-out whole-sample *n*, the "six of twelve" prose counts, and — the informative one — `test_har_rvs_marginals_under_the_grouped_nulls_are_the_artifacts`, which follows `leave_one_year_out.model` out of the artifact and reports that the README no longer quotes the artifact's **`auto_arima`** marginal. The guard names the model substitution rather than silently comparing the wrong model's numbers.
  - **The `if: always()` upload published a decoy. Fixed 2026-08-17.** `aurex direction` writes `public-data/direction.json` once, at the end. A cancelled run therefore leaves the *checked-out* copy in the workspace, and the upload ships that: the artifact from that run is byte-identical to the one committed on `main` (sha256 `f1a34f34…`, re-hashed and confirmed). `if-no-files-found: error` protected nothing, because the file existed. An artifact named `direction` from a run that computed nothing is indistinguishable from one that computed everything unless you hash it — the §1 signature failure exactly. All three affected jobs now delete their own target after checkout and before the compute, and **fail if the path is not there to delete**: a mistyped path would make the deletion a silent no-op and restore the decoy while looking like the repair, and exactly one of the delete step and the upload step can be right about the filename. The deleted file's sha256 goes to the log as the decoy's fingerprint. `shootout.yml` (`benchmarks.json`) and `factors.yml` (`factors.json`) had the identical bug and are fixed in the same commit; `nightly.yml` never had it, because it commits rather than uploading.
  - **The step numbers in this section are GitHub's, and the delete step shifted them.** GitHub counts "Set up job" as step 1. On `main` before the fix: 6 grade, **7 guard, 8 README**, 9 upload, 10 commit — which is the numbering used above. With the delete step inserted, add one to everything from the compute onwards; on this branch, which also carries the cache pre-flight, the guard is step 9 and the README test step 10. **Refer to them by name.**

- **`direction.yml` on this branch was never runnable — fixed 2026-08-17 by making an empty `cache_dir` mean "resolve live".** Commit `2f824fc` made the job resolve offline from `AUREX_CACHE_DIR`, with `cache_dir` defaulting to `tests/fixtures/bench-cache` — and **that directory has never existed in this repository** (`git log --all --diff-filter=A` finds nothing; the only occurrence of the string anywhere was the default value itself). The pre-flight step would have exited with "no cached xauusd" before any compute, and never did, because the only run this workflow has ever had used `main`'s live-resolution version. The committed fixture that does exist, `tests/fixtures/seed-cache`, holds 300 rows from 2025-05-21 and is correctly refused by the same step's two remaining checks — it starts after `--from 2015-01-01`, and its 434-day span is short of the 500 observations the volatility models need.
  - **The repair keeps both paths rather than dropping either**, which is why it was preferred to reverting the offline hunk: the offline design is *right* when a cache exists — this job applies no freshness guard, so §6 permits one, and it drops the LBMA dependency that fails roughly half the time from a runner. Reverting would also have discarded the pre-flight and the `resolution.*` provenance, both of which tested correct. So `cache_dir` now defaults to empty and means live; naming one goes offline against it and runs the pre-flight, which is `if:`-gated on the input.
  - **The guard now checks provenance against the mode the run was dispatched in.** It asserted `resolution["offline"] is True` unconditionally, which would fail every live run; it now compares against `inputs.cache_dir != ''` and fails when the artifact's account of itself disagrees with how the job was launched, in either direction. Verified on all four combinations (offline artifact / live artifact × cache named / not) before commit.
  - **Committing a cache that would restore an offline default is still a maintainer decision**, and it needs the LBMA history question settled first. Nothing here commits one.
  - **And the two paths do not grade the same sample.** `SourceChain.load` with `offline=True` returns `self.cache.read(series_id)` — the whole cached series, `start` and `end` ignored — while the live path returns what the loader fetched for the requested window. `aurex direction` asks for `2015-01-01 − DEFAULT_LOOKBACK_DAYS//2`, i.e. 2005-01-02. On a cacheless runner a live run therefore sees 2005-01-04 onwards (5,408 observations to 2026-07-29, which is exactly what the committed artifact records); the same command run offline against the local `.cache`, which reaches back to 2000-01-04, sees 6,662. `session_limit` is `None` for this asset, so every fit uses the whole expanding history and **every number moves**. The window count does not — it is anchored on `--from` — but the fits behind it are not the same fits. An offline artifact and a live artifact with identical flags are not comparable unless the cache is trimmed to the requested start.
- **`--step 10` is not a smaller version of the `--step 5` run. At ten sessions it is the `--step 5` run's own thinned subsample, exactly — and it changes which model the controls interrogate.** Measured 2026-08-17 by running `--step 10 --to 2026-07-29` against a cache trimmed to the live path's start, so the sample is the committed artifact's in every respect but the step: 5,408 observations, 2005-01-04 to 2026-07-29, 290 windows against 580, 1,439 scored forecasts against 2,876. (The window counts are exact, not approximate: 290+290+289+286+284 = 1,439 and 580+579+577+572+568 = 2,876.)
  - **The identity.** At `--step 5` the h=10 screens are thinned with stride 2 — every other window, i.e. every ten sessions — which is precisely the set of as-of dates a `--step 10` run visits. Same dates means the same `position`, the same seed (`seed + position × 1000`), and therefore the same ensembles. So the step-10 **full** screen at h=10 is the step-5 **thinned** screen, and it reproduces it to every emitted digit: equal-width *p* = 0.4309 and equal-count *p* = 0.0252 in both, with every model's resolution identical. h=10 is the *only* horizon where this holds (at h=5 the step-5 run does no thinning at all; at 21, 42 and 63 the two strides are not nested), and it is the horizon the README's one rejection lives at.
  - **So the README's h=10 row loses its second run.** At step 10, `stride = ceil(10/10) = 1`, so thinning is a no-op and the "thinned" screen is the full screen. The published pair (0.036 full / 0.025 thinned) becomes (0.025 / 0.025): the same test counted twice. `both_screens_reject_equal_count` stays `true` while meaning half of what it meant. **That is a change in what the check is, not a loss of power**, and it applies to the whole controls block — every thinned control at h=10 is unchanged to the digit, and every full one moves onto its thinned twin (placebo *p* 0.9636 → 0.5465, placebo rank 7 → 5, within-year permutation 0.0138 → 0.0238, cyclic shift 0.0132 → 0.0284).
  - **The hazard, and it is the §1 shape.** Leave-one-year-out follows whichever model the *published* equal-count screen ranks first. At step 5 that is HAR-RV (resolution 0.01097, marginal *p* 0.0069). At step 10 the published screen is the thinned one, where **AutoARIMA outranks it** (0.02050 at *p* 0.0046 against HAR-RV's 0.01907 at 0.0070) — so the step-10 artifact's twelve-row table is AutoARIMA's, not HAR-RV's. Read as though the column were unchanged, it says the effect got much stronger and 2017 stopped mattering: refits rejecting marginally 6 → 11, at set level 2 → 8, and *without 2017* 0.6528 → **0.0038**. Every one of those is a different model on a different screen. **`--step 10` is therefore not a safe way to fit inside the six-hour cap for any run that feeds the README's direction controls** — it is safe for the horizon grid and the per-model tables, and it silently substitutes the subject of the section's central caveat.
  - The README guard catches it, which is the one reassuring part: `test_har_rvs_marginals_under_the_grouped_nulls_are_the_artifacts` follows `leave_one_year_out.model` out of the artifact and fails with "the README no longer quotes the artifact's **auto_arima** marginal" rather than silently comparing the wrong model's numbers.
  - **A smaller gap found on the way.** `reproduce` omits `--offline` and the cache directory, so an offline artifact's own command would re-resolve live — and per the entry above, live and offline do not grade the same sample. The information is in the artifact (`resolution.cache_dir`, `resolution.series_end`, and the guard requires both), but the `reproduce` string alone is not sufficient to reproduce an offline run.

- **Platform drift is real, small, and confined to the models that fit something. Run 32044823578 (hosted, `--step 10 --to 2026-07-29`, `commit=false`) against the same command on a Mac mini.** Both artifacts resolve the identical sample — 5,408 observations, 2005-01-04 to 2026-07-29 — and identical windows, so identical `position` values and therefore identical seeds. The realised outcomes (`realised_rate` at every horizon) are the same to the digit, which is the check that the outcome side carries no drift at all. 735 of 4,868 comparable leaves differ (15.1%), and they sort cleanly by how much fitted state a model carries:

  | Model | What it fits per window | `mean_forecast` moved at | max abs Δ Brier |
  |---|---|---|---|
  | `nhits` | 200 training steps, torch | **all five horizons** | 0.0039 |
  | `auto_arima` | order search + MLE, statsforecast | h=10, 42 | 0.00009 |
  | `chronos_t5_small` | nothing — pretrained, inference only | h=10 | 0.00007 |
  | `gjr_garch` | bounded MLE, SciPy | none | 0.00001 |
  | `har_rv` | OLS on realised variance | none | **0** |
  | `random_walk_drift_matched` | nothing | none | **0** |

  - **HAR-RV and the null are bit-identical across the two machines**, and GJR-GARCH is within one unit in the fifth decimal. So the FHS path is reproducible across architectures and the gradient-descent path is not: NHITS drifts by roughly forty times more than anything else, and its equal-count resolution at h=10 nearly halves (0.013603 → 0.006944). Chronos moving barely at all while NHITS moves everywhere is the distinction worth keeping — **inference reproduces, training does not.**
  - **The drift leaks into every model's screen statistics even where its forecasts did not move**, because the screen studentises each model against a null resampled over the whole set and takes a maximum across it. `har_rv`'s own numbers never change, but its *rank* does, in ten places. A per-model figure can therefore be platform-stable while the set-level p-value quoting it is not.
  - **On the one cell the README's finding lives in**, the two effects are cleanly separable and the sample effect dominates: equal-count set *p* at h=10 goes 0.0364 (committed, step 5) → 0.0252 (step 10, same machine) → 0.0282 (step 10, hosted). **Sample −0.0112, platform +0.0030**, about 3.7 to 1. HAR-RV's own resolution at that cell — 0.019073 — is identical on both machines, so every README figure about HAR-RV is platform-stable and moved only because the sample changed. Leave-one-year-out's headline count moves on both axes: 6 refits rejecting marginally → 11 (sample) → **12** (platform), so a published count is one unit sensitive to the architecture it ran on.
  - **What this settles for the README guard.** `tests/test_readme_direction.py` compares at the artifact's own precision with tolerances between 1e-9 and 6e-6. Those are tighter than the platform drift measured here on four of six models, so **a README regenerated on one machine and re-checked on another will fail on NHITS and AutoARIMA figures even with the sample held fixed.** That is not an argument for loosening the tolerances — it is an argument for regenerating the artifact and the README together, on whichever machine, and never mixing figures from two runs.
- **`calibration-gold.json` cannot be reproduced from its own recorded command, and the reason is not the `dirty` flag. Do not regenerate it until this is fixed.** Attempted 2026-08-17 from a clean tree with the artifact's own `reproduce` string — `uv run aurex score --asset gold --from 2015-01-01 --to 2026-07-29 --step 5 --horizons 5,10,21,42,63 --paths 4000` — to make the provenance line honest. It ran in 70s and **1,975 of 8,181 leaves changed.** The regenerated artifact was discarded and the committed one restored; nothing was published.
  - **The as-of dates are identical and the sample is not.** Both runs score 580 windows at h=5, first 2015-01-02, last 2026-07-21. What moved is the history *behind* each fit: the committed artifact resolved the series from **2006-08-04** (5,009 observations) and the re-run from **2000-01-04** (6,662). `session_limit` is `None` for this asset, so every expanding-window fit sees six and a half more years, and every number built on one moves with it. **CRPS skill at h=5 went +0.0061 to +0.0026** — the headline figure, more than halved, from a command that is supposed to reproduce it.
  - **Mechanism, and it is the one already recorded for `direction.yml` one entry above.** `SourceChain.load` returns `cache.merge_write(fetched).frame` — the *union* of everything ever cached on that machine — and `start` never clips the result. `aurex score` asks for `2015-01-01 − DEFAULT_LOOKBACK_DAYS//2` = 2005-01-02 and gets whatever the local cache happens to reach back to. Three runs of the same resolution path have now returned three different sample starts: **2006-08-04** (this Mac, 2026-08-07), **2000-01-04** (the same Mac today, after the cache was extended backwards), and **2005-01-04** (a cacheless hosted runner, direction run 32044823578, which is the only one that got what the code asked for). The sample start is a property of the machine's cache history, not of the command.
  - **So the provenance line is already telling the truth, and understating it.** "working tree modified — not reproducible from that commit alone" is correct; what it does not say is that the run is not reproducible from the *command* either, on any machine whose cache differs. Regenerating would have swapped a true-but-incomplete provenance line for an honest-looking one attached to numbers that silently disagree with every published figure — including the README's headline skill range — which is the §1 signature failure with better paperwork.
  - **The fix is in the data layer, not in the artifact.** Either `chain.load` clips the returned frame to the requested `[start, end]`, or the resolved series start is recorded in `reproduce` as an explicit bound the command can replay. The first is the honest one and is a one-line behavioural change with a wide blast radius: **every published artifact's numbers would move once, deterministically, and then stay put.** That is a maintainer decision because it restates published results, and it should be made before any calibration artifact is regenerated for any reason. Until then the `dirty: true` line stands and is the more informative of the two problems it is pointing at.
  - **The clip is now implemented and the restatement is not. 2026-08-19, branch `clip-load-window-and-robots-position`, not merged.** `SourceChain.load` clips on every path out of it — live, cache-fallback and offline alike — and rebuilds `meta` from what it returns, so `rows`, `start` and `end` describe the frame the caller was handed rather than the cache behind it. The store is untouched and still accumulates: the superset is the point of the cache, and the return value being a superset was the bug. Two consequences chosen deliberately: a source that answers with nothing in the window is recorded as a decline and the chain moves on, and an offline load whose cache holds nothing in the window raises instead of serving a frame from another decade. That second one caught a test which had been passing for the wrong reason — `test_a_lens_with_no_overlapping_dates_reports_an_empty_block` arranged its non-overlap by putting the FX series in 2020 and the prices in 2026, which now fails at the chain rather than reaching the lens, so the fixture moved the non-overlap inside the window.
  - **What moves, measured rather than predicted, and nothing regenerated.** Every figure below is committed-artifact against a rerun of the artifact's own `reproduce` command with the clip in place, run `--offline --dry-run` against the local cache so the clip is the only thing that changed. **No artifact has been regenerated or committed; the restatement is still a separate decision.**
    - `calibration-gold.json`: sample **5,009 observations from 2006-08-04 → 5,408 from 2005-01-04**, window count and 2,876 scored forecasts unchanged. CRPS skill by horizon **+0.0061 → +0.0045, −0.0044 → −0.0048, −0.0012 → −0.0030, +0.0031 → +0.0034, +0.0094 → +0.0082**, so the README's published range −0.4%…+0.9% becomes **−0.5%…+0.8%**. DM still rejects nowhere; the smallest *p* in the table moves **0.23 → 0.32** (h=10 thinned). PIT KS still rejects nowhere (h=5 0.145 → 0.075 is the closest approach). **The chi-square gains a second rejection**: h=5 0.011 → 0.0014 and h=63 **0.058 → 0.037**, so "a chi-square rejects at one of them" becomes two, in the README's abstract, its §3a table discussion and its limitations list. 95% breaches at h=5 **19 → 18** with Kupiec *p* **0.043 → 0.025**; at h=10 9 → 10 and at h=21 2 → 3, both moving away from rejection. Skill-decay slope +0.0015 → +0.0016 (*p* 0.73 → 0.72, R² 0.08 → 0.10). The tail decomposition's headline splits **91%/9% → 96%/4%** volatility level against tail shape, because the engine-σ Gaussian rises (0.007405 → 0.007481) while Aurex falls (0.007906 → 0.007720) on the 9.37%/h=10 row. Direction-at-63 resolution **0.00200 → 0.00017**. 1,952 of 8,160 leaves change; every realised outcome — base rates, event counts, `realised_rate` — is identical to the digit, which is the check that the outcome side carries no sample dependence at all.
    - `direction.json`: **nothing moves.** It was computed on a cacheless runner, so it already records the window the code asked for — 5,408 observations, 2005-01-04 to 2026-07-29 — and the clipped local cache resolves that same window to the day and the count. What the clip changes here is that the Mac can now reproduce it: unclipped, the same command on this machine resolves 6,662 observations from 2000-01-04.
    - `factors.json`: **nothing moves, and this was measured rather than reasoned.** Clipped and unclipped runs of the artifact's own command against the same cache are identical on all 820 leaves. The reason is that `aurex factors` asks from 2000-01-01 and no cached series here begins before it, so the clip has nothing to cut — which also means the result is a property of this cache, not a guarantee. A machine whose cache reached back to 1995 would see factors move too. (Against the *committed* artifact 93 leaves differ, all of it data drift between the 2026-08-12 live run and today's cache, none of it the clip.)
    - `latest.json` and `live-log.json`: **nothing moves on the nightly**, which runs cacheless, so `chain.load` was already returning exactly `[end − 20y, end]`. A *locally* run nightly changes, and changes toward the runner's numbers.
    - `routes.json`: no series behind it. Unaffected.
    - `benchmarks.json`: same sample move as calibration — 5,009 from 2006-08-04 → 5,408 from 2005-01-04 — so its figures move. See the entry below.
  - **The README tests are blind to this by construction, and that is the finding.** `tests/test_readme_direction.py` and `tests/test_readme_factors.py` compare the README against the *committed* JSON cell by cell and never regenerate anything, so a change in what the published command resolves leaves both green while every published figure moves. `tests/test_reproduce.py` is the missing half: it runs `aurex score` on the synthetic asset, deepens the cache behind its back the way a nightly or another machine would, re-runs **the artifact's own `reproduce` string parsed out of the artifact**, and asserts the resolved bounds, the observation count and the whole `calibration` block are unchanged. Verified to fail without the clip — both the sample assertion and the numbers assertion — and it carries a third test asserting the deepened cache really does reach behind the resolved sample, so the fixture cannot quietly stop being able to catch anything.
  - **Found while measuring, and not caused by the clip: the README's tail-shape count disagrees with the artifact it describes.** "Seven of the nine rows put Aurex below the engine-σ Gaussian" — the committed artifact says **eight of nine**, on its own six-decimal figures, with 9.37% at ten sessions the only row above. The neighbouring sentence, "only the two deepest rows come out fatter", is true against the *sample-σ* column and not against the engine-σ column the previous sentence names. The clip does not change the count: it is eight of nine before and after. Nothing in the suite checks the calibration section of the README against `calibration-gold.json` — the mechanical README tests cover direction and factors only — which is how a hand-counted figure survived in a document whose whole claim is that its figures are tied to artifacts.
- Four skip records (2026-08-06, 08-08, 08-10, 08-11) state the wrong cause. **They have not been rewritten and must not be** — their `verdict` fields were correct throughout; only the summarising sentence was wrong. The README names them in *Track record integrity* and explains why the misstatement was the dangerous kind: it pointed the reader at the freshness tolerance, which is the one knob that must not move.
- **Every Yahoo series is fetched against a blanket `Disallow`.** `query1.finance.yahoo.com` and `query2.finance.yahoo.com` — where `yfinance` fetches — serve exactly `User-agent: * / Disallow: /` (26 bytes, verified 2026-08-17 with Aurex's own client). `YahooLoader` delegates transport to `yfinance`, which never consults `robots.txt`, so `usdinr` (blocking), `xau_futures` and `vix` have resolved that way for the whole build. This is recorded here because the probe workflow that found it is throwaway and the finding must outlive it. Two honest options and no third: stop using Yahoo, or decide deliberately and in writing that this one is fetched anyway. `finance.yahoo.com` itself does **not** disallow the quote pages; only the API hosts do.
  - **Decided 2026-08-19, and the constraint is in §6: an explicit `Disallow` binds; an ambiguous 401/403 on `robots.txt` itself is unknown, not permission.** The brief that costed it is `docs/robots-position.md`, decision in its §0. What it settles: stooq and `fsapi.gold.org` stay excluded, Yahoo joins them and the three series it serves are now a deadline rather than a footnote, LBMA's 401 is disclosed-and-asked with the single nightly fetch continuing until they answer, and egress re-rolling is refused under every position. What it does not settle is the replacement for `usdinr`, which is blocking. The brief as originally written: It costs both positions, names which series break and which candidates reopen under each, and states the asymmetry plainly — stooq was struck from the candidate list for serving the same 26-byte file Yahoo serves us nightly, and the difference was transport rather than principle. Three things it adds that are not above. **First,** binding Aurex generally does not begin with Yahoo: the 401 at `prices.lbma.org.uk` makes `xauusd` non-compliant too, so that position stops every published price. **Second,** reading the loaders shows the guard governs **2 of the 11 series** this project loads — `gpr` and `ibja_gold`. Six go through `check_robots=False`, and five of those are `FredLoader`, which passes the flag with **no recorded reason at any call site**; that flag needs a reason or removal whichever position wins. **Third,** nobody has read `fred.stlouisfed.org/robots.txt`, and seven series depend on it, so the strict position cannot be fully costed until someone does.
- **Probe round 5, 2026-08-19 — candidates for the three Yahoo series. Run from this Mac, not a runner, which is the one thing it does not measure.** Five attempts per arm, LBMA as the control every round has carried, artifacts in the session scratchpad rather than the repository. Yahoo was not fetched: it is excluded on principle now, not on measurement.
  - **Control, LBMA `gold_pm.json`: 5/5 clean, one body, median 0.52s, last observation 2026-08-17 (lag 1), 14,663 observations back to 1968-04-01.** `robots.txt` still answers **401** and Aurex's checker still returns `False` for it. Residential egress, so this says nothing about the runner failure rate; it says the endpoint and the history are unchanged.
  - **The RBI reference rate is FBIL's, and finding that out is most of the answer.** `www.rbi.org.in/Scripts/BS_DisplayReferenceRate.aspx` still serves the series but its last observation is **2018-07-09**, and the page itself says computation and dissemination passed to Financial Benchmarks India Ltd on 2018-07-10. FBIL's site is an Angular app; the endpoints it calls were read off its own network traffic in a browser: **`https://www.fbil.org.in/wasdm/refrates/fetch?authenticated=false`** (latest, JSON) and `…/wasdm/refrates/fetchcommongraph/{period}` (history). That is how the endpoint was found — from the publisher's own client, not from a third-party wrapper.
    - **Transport:** 5/5 clean, median **0.144s**, one body. Nothing in front of it.
    - **Last observation: 2026-08-11, against a run date of 2026-08-18 — seven calendar days, five Mumbai business days.** `usdinr` declares `max_lag_days=4`, so as reached today this source is refused by the freshness guard **every night**. The 6-month history is complete for Mumbai business days (117 observations, 11 gaps, every one a plausible Indian market holiday) and then simply stops, which is what a *delayed* public feed looks like and also what a *stalled* one looks like. The query string literally says `authenticated=false`. **Which of the two it is has not been established and must not be guessed** — the test is cheap and takes days rather than requests: re-read the endpoint on successive mornings and see whether the newest observation advances by one business day each time.
    - **History depth: 3M and 6M answer; 1Y, 5Y, 10Y and MAX all return HTTP 500.** So the discovered endpoints reach back **six months**, not to 2015. A source for the published sample would need an archive route nobody has found yet.
    - **Fixing time:** the payload states it, so it is quoted rather than inferred — `displayTime` is `13:00:00` on each record.
    - **Agreement:** against the cached `INR=X` closes on the 117 overlapping days, mean **−0.2 bps**, median +2.1, p95 |Δ| **62.5 bps**, max 120.7. A different quote at a different hour, not a different rate.
    - **Robots: not measured. `www.fbil.org.in/robots.txt` read-timed-out at 20s on every attempt** while the data endpoints answered in 144ms. Aurex's guard fails open on an unreachable file, so it would fetch. Under §6 that is an unmeasured host, not a permitted one.
    - **Licence: nothing granted.** The only statement anywhere on the site is the footer, "Copyright 2020. All Rights Reserved". Under the provenance rule that is a `cite_as` we do not have and a licence we cannot claim.
  - **ECB daily reference rates — the second arm, and the definitional difference is smaller than expected.** `eurofxref-daily.xml` and `eurofxref-hist.xml`, both allowed by a `robots.txt` that is served and specific. 5/5 clean, median 0.441s. Last observation **2026-08-18, lag 0** — it is the only candidate here that reaches the run date. History: 7,073 days from 1999-01-04, but **INR starts 2009-01-02**, so it covers the published sample from 2015 and not the pipeline's 20-year lookback. Weekdays only, no weekend carry-forward.
    - **The cross, measured rather than waved at.** USD/INR is not published; it is `INR ÷ USD` from two EUR reference rates fixed at the same moment. Against the cached `INR=X` closes over 2,972 overlapping days from 2015-01-02: mean **−0.91 bps**, median **−0.02 bps**, sd 31.5, p95 |Δ| **64.5 bps**, max 233.6 (2023-11-02). So the cross carries **no measurable level bias** and about ±0.6% of timing noise at the 95th percentile — it is a different observation of the same rate, taken at a different hour, not a different definition of the rate.
    - **Two costs, both stated by the ECB rather than by us.** The rates come from the daily concertation at **14:10 CET** and publish **around 16:00 CET**, and the page says in terms: *"The reference rates are published for information purposes only. Using the rates for transaction purposes is strongly discouraged."* Aurex does not transact, but a rupee price built on them inherits that sentence and would have to carry it.
    - **And the one that is ours: `robots.txt` asks `Crawl-delay: 5` and `HTTP_COURTESY_DELAY` is 1.0s.** Nothing in the codebase parses `Crawl-delay`, so a wired ECB loader would fetch five times faster than asked. This probe honoured it by hand. That gap was recorded as "small and separate" when only FRED's 1s was in view, where we comply by coincidence; against ECB we would not comply at all, and §6's rule about honouring what a host states does not have an exception for the parts of the file we never implemented.
  - **FRED `VIXCLS` — the one candidate that is ready, and the cadence is the reason.** Robots: allowed, measured. 9,253 observations from **1990-01-02** to 2026-08-17, weekdays only, no weekend carry-forward. **Cadence, read off ALFRED vintages the way `DEXINUS`'s was: 45 consecutive vintages, 31 advances, spread Mon 7 / Tue 6 / Wed 6 / Thu 6 / Fri 6 and none on a Saturday or Sunday.** That is daily publication, against `DEXINUS`'s 11 advances in 76 vintages every one of them a Monday — the two FRED series behave completely differently and the difference is exactly what made `usdinr`'s fallback nominal.
    - **Lag is set by the posting hour, not by the cadence.** FRED's own stamp for the 2026-08-17 observation is **2026-08-18 08:37 CDT = 13:37 UTC**, and a fetch at ~20:00 UTC on 08-18 still showed 08-17 as newest. A 02:00 UTC nightly therefore sees the value from **two days back midweek and four after a weekend**, because the day's posting lands eleven hours after the run. `vix` is not blocking and declares no tolerance, so nothing refuses; if it ever becomes blocking, that is the number to declare against.
    - **It is the same series as `^VIX` to the published precision, and it is not the same calendar.** Over 2,923 overlapping days from 2015, the median absolute difference is **0**, two days differ by more than 0.05, and one — 2026-02-06, FRED 17.76 against Yahoo 20.37 — differs by 2.61. But FRED carries **31 observations Yahoo does not**, and all 31 are US market holidays (Memorial Day, Juneteenth, Independence Day, Labor Day, Thanksgiving, MLK, Presidents' Day), each with a value that differs from the previous session rather than being carried forward. A realised-volatility estimator fed the FRED calendar is fed 31 extra sessions over eleven years. That needs understanding before it is wired, and it is a difference in what the series *is*, not in its quality.
    - **Licence, from FRED's own series page, fetched with Aurex's client:** source Chicago Board Options Exchange, `"license": "https://fred.stlouisfed.org/legal/#copyright-citation-required"`, tagged *copyrighted: citation required*. So a wired loader carries a `cite_as`, which is the first candidate in this project that states one.
  - **What this leaves.** `vix` has a compliant, measured replacement that only needs wiring and a calendar decision. `usdinr` — the blocking one — has **no compliant source that is both fresh enough and deep enough**: FBIL is seven days behind and six months deep as reached, ECB is fresh and deep enough for the published sample but is a cross, is discouraged for transactional use, and asks for a crawl delay we do not implement. `xau_futures` has no candidate at all in this round, and losing it removes measured OHLC and with it HAR-RV. **No loader was wired, no chain was re-pointed, and no tolerance was touched.**

- Five pre-existing `mypy --strict` errors in `aurex/score/shootout.py` and `bench/`. Present on `main` before this work.
- `bench/` sits at 0% coverage, deliberately. **Do not add an omit** — a visible zero on 192 statements is information. The gate passes at 89%.

---

## 6. Standing constraints — do not violate

Each of these exists because violating it was tempting at some specific moment.

- **An explicit `Disallow` is honoured; an ambiguous refusal is unknown, and unknown is not permission.** Decided 2026-08-19; the costing and the reasoning are `docs/robots-position.md`, §0 for the decision. Where a host serves a `robots.txt` naming a path Aurex fetches, that binds Aurex — crawl or single retrieval, and whether or not the data is unobtainable elsewhere. `stooq.com` and `fsapi.gold.org` stay excluded on that ground and are not re-examined. **`usdinr` (blocking), `xau_futures` and `vix` are in breach of this from the day it was written**, because `yfinance` fetches `query1/query2.finance.yahoo.com`, which serve `User-agent: * / Disallow: /`, and never consults the file; that is a deadline, and the replacements are still being measured (§5, probe round 5). Where a host answers **401 or 403 on `robots.txt` itself**, nothing has been stated to us: behind a CDN that is as likely an edge artifact as a policy. Read as refusal it stops every published price on an unverified inference; read as permission it is the convenient reading of a silence. So it is **unresolved**, and unresolved is handled by asking — disclose it wherever the source is described, write to the administrator, continue the *single* nightly fetch unchanged while the enquiry is open, and stop the day they say stop. `prices.lbma.org.uk` is the only host in that state, `LbmaGoldLoader`'s `check_robots=False` names that one question and must not be copied to a host that publishes an explicit `Disallow`, and `FredLoader`'s came out because it was measured unnecessary. **No re-rolling of egress addresses, under any position** — retrying a job until it draws an address a CDN does not challenge is evasion, not a source, and disclosure cannot cure it. §4a of the position document stays costed and unbuilt.
- **A source's robots status is measured with Aurex's own client before the source is excluded, and the guard fails open on statuses it does not recognise.** The candidate list was once shortened by a check reading somebody else's `robots.txt` — `urllib` got a 404 from stooq where Aurex gets a `Disallow`. The remaining hole is measured and unclosed: `www.rbi.org.in/robots.txt` answers **HTTP 418** with a WAF block page, and `robots_allows` returns **True** for it, because only 401 and 403 are read as withheld. An ambiguous status that reads as permission is the §1 signature failure, and it is the same ambiguity the LBMA 401 is handled under — it should be treated the same way and currently is not.
- **Never widen a freshness tolerance to make a run pass.** On all four skipped nights it would have published nothing truer and would have retired the signal that the anchor series has a single point of failure.
- **Never make `d_geopolitical_risk` optional again.** It is `required=True` so attribution refuses rather than degrading. A dropped-out reason recorded in a field nobody reads is not a defence against an inverted sign.
- **Never sum a chain response with a factor loading.** Crude is in both; they are alternative decompositions of overlapping paths. A test asserts no field in the emitted block is named like a total.
- **Never rewrite an elapsed forecast or a skip record.** Correct in code and note the correction in the README. Same rule that governs the withdrawn +4.6%.
- **Seed cache is correct for `factors.yml`, wrong for `nightly.yml`.** `aurex factors` applies no freshness guard and makes no claim about today's price, so a cached series ending weeks ago runs correctly and dissolves nothing. The nightly is the opposite: a cache lets a stale copy resolve, which the guard then correctly refuses. Seeding it buys nothing honest. **If `factors.yml` runs from a cache, the artifact must name the cache's end date** — a run reproducible only from a cache nobody can see is the `--to` problem wearing a different hat.
- **Bound every published run with `--to`.** Default to the series' own last observation, never to today. The artifact records requested *and* resolved bounds plus the exact reproducing command. **`--to` bounds the end and nothing bounds the start** — see §5 — so a rerun is not a reproduction until `sample.resolved.from` and `sample.observations` have been diffed and found equal.
- **A page names every artifact it is built from.** The calculator renders its distribution from one file and its breakeven table from another, and reported only the first: the hurdles — the numbers a reader would act on — sat under a provenance line belonging to a different artifact, with the "working tree modified" warning that was theirs invisible. Provenance attached to something other than what is on the page is the decoy failure in a different costume. `Footnote` takes `also` for exactly this; if a page gains a third source, it names three.
- **Pre-register before running.** Every graded pre-registration so far has been more useful than the number it predicted, including the two that missed. Leave the prediction as written when it misses; explain the miss.
- **Leak guard:** no asset name and no jurisdiction code (uppercase alpha-3, case-sensitive) as a literal outside `assets/` and the routes table — including in `web/` (`.ts`, `.tsx`, `.css`). It has already caught a driver named in a UI caption.
- **Provenance:** every external fact carries `source_url` and `source_confidence`. `primary` = the publisher's own file; `secondary` = a redistributor (FRED, Yahoo). `cite_as` is filled only where the publisher states one. Never mark something primary without reading the primary document. Where a schedule does not know a value, declare a **gap** rather than carrying the last value forward — the fuel-excise schedule does exactly this for Oct 2018–Nov 2021.
- **HAR-RV must be unavailable for any leveraged route.** Deterministic variance iteration means every path shares one trajectory, so barrier and liquidation statistics from it silently assume constant volatility.
- **Multiple comparisons.** Six models against one null at α = 0.05 finds a winner whether or not one exists. Use Hansen's SPA or the Model Confidence Set; per-model DM statistics are description, not decision.

---

## 7. If you need more detail

The README is the source of truth and is long (~750 lines) and heavily annotated — methodology, results, limitations, and the corrections made along the way. The agent's memory files in the Claude Code project memory directory encode the same standards as short rules and have been audited for drift.

Four spec addenda exist as separate documents from the design conversation: §15–16 (USD lens, asset abstraction — both built), §17 (oil module — not built), §18–19 (oil as a traded instrument, India transmission chain — chain built, oil not), §20 (routes and jurisdictions — built).

---

**Last note.** The most credible thing in this repository is not any positive result. It is the collection of negative ones that survived contact with the data, published with the arithmetic that proves them, alongside a visible record of the claims that were withdrawn when they did not. Protect that. It is worth more than any finding this project could produce.
